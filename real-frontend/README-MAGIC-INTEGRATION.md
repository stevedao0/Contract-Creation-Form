# Magic Integration Workspace

This folder is the real frontend app copied for side-by-side Magic Pattern layout integration.

- Magic Pattern repo root is reference only.
- Do not import demo/mock/raw data into `real-frontend`.
- Do not edit backend/API/DB/env from this workspace.
- Fixes must preserve real API calls, real data, real routes, and real actions.
- After verification, sync only safe changed frontend files back to `F:\APPs\frontend`.
